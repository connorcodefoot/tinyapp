<header>
  <head>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      rel ="additional stylesheet"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />

    <!--- Additional CSS -->
  <link rel="stylesheet" href="style.css">

  <title>URL - TinyApp Example</title>
  
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <div class="navbar-buttons">
        <% if(typeof user === 'undefined') { %>
          <button type="submit" class="btn btn-outline-primary" onclick="window.location.href = '/login';">Login </button>
          <button type="submit" class="btn btn-outline-secondary" onclick="window.location.href = '/register';">Register</button>
        </form>
        <% } else { %>
        <a class="nav-item nav-link" href="/urls"><%= user.email %></a>
        <div id = "navbar-buttons">
        <form method="POST" action="/logout">
          <button type="submit" class="btn btn-outline-secondary">Logout</button>
        </form>
        <div></div>
        <% } %>
      </div>
    </div>
  </nav>
</header>

